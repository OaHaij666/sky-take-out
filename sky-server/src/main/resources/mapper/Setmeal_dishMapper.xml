<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.sky.mapper.Setmeal_dishMapper">

    <!-- 批量插入套餐菜品关系 -->
    <insert id="insertBatch" parameterType="java.util.List">
        insert into setmeal_dish (setmeal_id, dish_id, name, price, copies)
        values
        <foreach collection="list" item="setmealDish" separator=",">
            (#{setmealDish.setmealId}, #{setmealDish.dishId}, #{setmealDish.name}, #{setmealDish.price}, #{setmealDish.copies})
        </foreach>
    </insert>
    
    <!-- 根据套餐id删除套餐菜品关系 -->
    <delete id="deleteBySetmealId" parameterType="Long">
        delete from setmeal_dish where setmeal_id = #{setmealId}
    </delete>
    
    <!-- 根据套餐id批量删除套餐菜品关系 -->
    <delete id="deleteBySetmealIds" parameterType="java.util.List">
        delete from setmeal_dish where setmeal_id in
        <foreach collection="list" item="setmealId" separator="," open="(" close=")">
            #{setmealId}
        </foreach>
    </delete>

</mapper>